<div class="col-lg-12 comment_edit_<%= @comment.id %>">
  <div class="form-group">
    <%= form_for @comment, url: comment_path(@comment.id), method: :put, remote: true do |f| %>

      <div class="field">
        <%= f.label :comment, 'コメント' %>
        <%= f.text_area :comment, size: '10x5', class: 'form-control' %>
      </div>

      <div class="actions text-right">
        <%= f.submit '更新', id: 'comment-submit',class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>
